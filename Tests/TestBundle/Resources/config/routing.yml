index:
    pattern:    /
    defaults:   { _controller:TestBundle:Default:index }

login:
    pattern:    /login
    defaults:   { _controller:TestBundle:Security:login }

login_check:
    pattern:    /login_check
